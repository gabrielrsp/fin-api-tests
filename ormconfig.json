{
  "password": "docker",
  "database": "fin_api",
  "type": "postgres",
  "port": 5432,
  "host": "localhost",
  "username": "docker",
  "entities": [
    "./src/modules/**/entities/*.ts"
  ],
  "migrations": [
    "./src/database/migrations/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}
